<div class="email-container">
  <!-- Header -->
  <div class="email-header">
    <h1>💰 Payment Required</h1>
    <p>Your service is ready - please complete payment</p>
  </div>

  <!-- Body -->
  <div class="email-body">
    <div class="greeting">Hi {{clientName}},</div>

    <div class="content">
      <p><strong>{{caName}}</strong> has completed your <strong>{{serviceType}}</strong> request and is waiting for payment.</p>
    </div>

    <div class="warning-box">
      <p><strong>⏰ Payment Pending</strong></p>
      <p style="margin-top: 10px; font-size: 14px;">Please complete the payment to receive your deliverables and finalize the service.</p>
    </div>

    <!-- Payment Details -->
    <div class="info-box">
      <div class="info-box-row">
        <span class="info-label">Service Type:</span>
        <span class="info-value">{{serviceType}}</span>
      </div>
      <div class="info-box-row">
        <span class="info-label">CA Name:</span>
        <span class="info-value">{{caName}}</span>
      </div>
      {{#if invoiceNumber}}
      <div class="info-box-row">
        <span class="info-label">Invoice Number:</span>
        <span class="info-value">{{invoiceNumber}}</span>
      </div>
      {{/if}}
      <div class="info-box-row">
        <span class="info-label">Amount Due:</span>
        <span class="info-value" style="font-size: 18px; font-weight: 700; color: #667eea;">{{currency amount}}</span>
      </div>
      {{#if dueDate}}
      <div class="info-box-row">
        <span class="info-label">Due Date:</span>
        <span class="info-value">{{formatDate dueDate}}</span>
      </div>
      {{/if}}
      <div class="info-box-row">
        <span class="info-label">Request ID:</span>
        <span class="info-value">#{{requestId}}</span>
      </div>
    </div>

    <div class="content">
      <h3 style="margin: 20px 0 10px; color: #333;">Secure Payment:</h3>
      <ul style="padding-left: 20px; color: #555;">
        <li>All payments are processed through secure payment gateway</li>
        <li>Funds are held in escrow until service completion</li>
        <li>Multiple payment methods accepted (UPI, Cards, Net Banking)</li>
        <li>Instant payment confirmation</li>
      </ul>
    </div>

    <!-- Call to Action -->
    <div style="text-align: center; margin: 30px 0;">
      <a href="{{paymentUrl}}" class="btn">Pay Now - {{currency amount}}</a>
    </div>

    <div class="divider"></div>

    <div class="content" style="font-size: 13px; color: #6c757d;">
      <p><strong>Payment Issues?</strong></p>
      <p>If you encounter any problems during payment, please contact our support team at support@camarketplace.com or call +91-XXXXXXXXXX</p>
    </div>
  </div>

  <!-- Footer -->
  <div class="email-footer">
    <p><strong>CA Marketplace</strong></p>
    <p>Connecting Clients with Trusted Chartered Accountants</p>
    <div class="social-links">
      <a href="#">About Us</a> |
      <a href="#">Help Center</a> |
      <a href="#">Contact Support</a>
    </div>
    <p style="margin-top: 15px; font-size: 12px;">
      © 2026 CA Marketplace. All rights reserved.
    </p>
  </div>
</div>
