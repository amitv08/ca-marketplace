/**
 * Escrow Auto-Release Job (TEMPORARILY DISABLED for testing)
 */

// Stub function for testing - escrow service temporarily disabled
export async function runEscrowAutoRelease(): Promise<number> {
  console.log('[Escrow] Auto-release job skipped (escrow temporarily disabled)');
  return 0;
}
